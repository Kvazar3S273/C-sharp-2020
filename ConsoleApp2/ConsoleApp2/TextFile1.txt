Розробити клас, що описує студента і зберігає: 
+прізвище,
+ім'я,
+по батькові,
+група,
+дата народження (DateTime),
+оцінки з наступних предметів (рваний масив) :
+програмування,
+адміністрування
+дизайн.
Додати методи та властивості по роботі з перерахованими даними: 
+конструктор (приймає прізвище, ім'я, по батькові та дату народження (DateTime) )
+встановлення оцінок з програмування ( метод, приймає масив оцінок )
+встановлення оцінок з адміністрування ( метод, приймає масив оцінок )
+встановлення оцінок з дизайну ( метод, приймає масив оцінок )
+середній бал з програмування ( read-only властивість, обчислюється на основі приватних полів )
+середній бал з адміністрування ( read-only властивість, обчислюється на основі приватних полів )
+середній бал з дизайну ( read-only властивість, обчислюється на основі приватних полів )
+середній бал успішності з усіх предметів ( read-only властивість, обчислюється на основі приватних полів )
+повне ім'я студента ( read-only властивість, обчислюється на основі приватних полів )
+вік (повних років) ( метод, приймає дату (DateTime) і обчислює вік студента на цю дату ( читайте про оператор віднімання )
+група ( властивість для читання і запису )
+очищення всіх оцінок (метод, без параметрів)